<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top shadow-sm">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">QLibrary</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "All Books", books_path, class: "nav-link #{'active' if current_page?(books_path)}", aria: { current: ('page' if current_page?(books_path)) } %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Books Category
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <li><%= link_to "Action", "#", class: "dropdown-item" %></li>
            <li><%= link_to "Another action", "#", class: "dropdown-item" %></li>
            <li><hr class="dropdown-divider"></li>
            <li><%= link_to "Something else here", "#", class: "dropdown-item" %></li>
          </ul>
        </li>
        <li class="nav-item">
          <%= link_to "Add Book", new_book_path, class: "nav-link #{'active' if current_page?(new_book_path)}" %>
        </li>
      </ul>
      
      <div class="d-flex align-items-center ms-auto">
        <form class="d-flex me-2">
          <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        </form>
        <div class="header-btns">
          <% if user_signed_in? %>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger" %>
          <% else %>
            <%= link_to "Signup", new_user_registration_path, class: "btn btn-outline-secondary me-2" %>
            <%= link_to "Login", new_user_session_path, class: "btn btn-outline-success" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>