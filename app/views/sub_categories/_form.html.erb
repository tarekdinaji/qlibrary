<%= form_with model: @sub_category do |form| %>
  <p>
    <%= form.label :title, "Title" %><br>
    <%= form.text_field :title %>
  </p>
   <p>
    <%= form.label :category_id, "Title" %><br>
    <%= form.text_field :category %>
  </p>

  <%= form.submit "Save" %>
<% end %>
